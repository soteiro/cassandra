info:
  name: get user by id
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/users/4
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
