info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "sote@mail.com",
        "password": "97158700"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.status == 200) {
          bru.setEnvVar("accessToken", res.body.access_token);
          bru.setEnvVar("refreshTokne", res.body.refresh_token)
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
