info:
  name: delete persona by id
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/api/personas/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
