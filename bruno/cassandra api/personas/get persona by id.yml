info:
  name: get persona by id
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/personas/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
