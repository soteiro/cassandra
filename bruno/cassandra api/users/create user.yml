info:
  name: create user
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/users
  body:
    type: json
    data: |-
      {
            "nombre": "jhon doe",
            "alias": "jhoasdasdn",
            "email": "jhon.sdfdfdoe@example.com",
            "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
