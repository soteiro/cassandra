info:
  name: Update User
  type: http
  seq: 1

http:
  method: PUT
  url: http://localhost:8080/api/users/3
  body:
    type: json
    data: |-
      {
        "nombre": "Juan Pérez Rojas",
        "alias": "juanpi_new_!¿new",
        "email": "juan.perez_rojas_new@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
