opencollection: 1.0.0

info:
  name: cassandra api
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  auth:
    type: bearer
    token: "{{access_token}}"
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
