info:
  name: list reflexiones by tipo
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/reflexiones
  params:
    - name: tipo
      value: memoria
      type: query
      enabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
