info:
  name: delete reflexion by id
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:8080/api/reflexiones/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
