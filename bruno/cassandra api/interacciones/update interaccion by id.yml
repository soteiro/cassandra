info:
  name: update interaccion by id
  type: http
  seq: 6

http:
  method: PUT
  url: http://localhost:8080/api/interacciones/1
  body:
    type: json
    data: |-
      {
        "interaccion": "Reunión de seguimiento reprogramada para el viernes a las 15:00 hs"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
