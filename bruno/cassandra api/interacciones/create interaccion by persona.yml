info:
  name: create interaccion by persona
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/personas/1/interacciones
  body:
    type: json
    data: |-
      {
        "interaccion": "Reunión de seguimiento sobre planificación trimestral y entregables"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
