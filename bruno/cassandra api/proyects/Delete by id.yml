info:
  name: Delete by id
  type: http
  seq: 8

http:
  method: DELETE
  url: http://localhost:8080/api/proyects/6
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
