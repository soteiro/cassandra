info:
  name: list proyect by user loged
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8080/api/proyects
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
