info:
  name: List Tareas por Estado
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/tareas
  params:
    - name: estado
      value: En Curso
      type: query
      enabled: true
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
