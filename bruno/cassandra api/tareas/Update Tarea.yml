info:
  name: Update Tarea
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/api/tareas/1
  body:
    type: json
    data: |-
      {
        "nombre": "Tarea Modificada con Bruno",
        "estado": "En Curso"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
