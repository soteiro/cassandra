info:
  name: List Tareas por Proyecto
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/api/proyects/3/tareas
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
