info:
  name: Get by ID
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8080/api/notas/3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
