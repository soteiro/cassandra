info:
  name: Delete
  type: http
  seq: 9

http:
  method: DELETE
  url: http://localhost:8080/api/notas/13
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
