info:
  name: Get all by proyect
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/api/proyects/1/notas
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
