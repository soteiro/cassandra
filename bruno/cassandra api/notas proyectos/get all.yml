info:
  name: get all
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/api/notas
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
