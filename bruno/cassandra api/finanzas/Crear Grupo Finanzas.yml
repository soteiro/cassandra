info:
  name: Crear Grupo Finanzas
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/api/finanzas/grupos
  body:
    type: json
    data: |-
      {
        "nombre": "Hogar"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
