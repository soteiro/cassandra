info:
  name: Actualizar Banco
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/api/finanzas/bancos/1
  body:
    type: json
    data: |-
      {
        "nombre": "Tenpo Crédito",
        "tipo": "credito"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
