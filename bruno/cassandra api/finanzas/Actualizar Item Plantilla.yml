info:
  name: Actualizar Item Plantilla
  type: http
  seq: 17

http:
  method: PUT
  url: http://localhost:8080/api/finanzas/plantilla/1
  body:
    type: json
    data: |-
      {
        "monto": 140000,
        "estado": "completado"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
